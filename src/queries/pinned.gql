query Pinned ($login: String!) {
  organization(login: $login) {
    pinnedItems(first: 6) {
      nodes {
        ... on Repository {
          id,
          name,
            description,
            primaryLanguage {
              name,
              color
            },
            stargazers {
							totalCount
            }
          }
        }
    }
  }
}